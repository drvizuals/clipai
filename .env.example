# Public client keys (safe to embed in the app via expo-constants)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Edge Function secrets (set via `supabase secrets set`, never bundle in the client)
ANTHROPIC_API_KEY=sk-ant-...
